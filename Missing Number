class Solution(object):
    def missingNumber(self, nums):
        n=len(nums)
        total_sum = sum(nums)
        actual_sum = n*(n+1)/2
        return actual_sum-total_sum
        """
        :type nums: List[int]
        :rtype: int
        """
        
